import { ViewStyle } from 'react-native';
declare const styles: {
    animatedContainer: {
        position: "absolute";
        width: string;
        alignItems: "center";
        justifyContent: "center";
        zIndex: number;
        elevation: number;
        top: number;
        left: number;
        backgroundColor: string;
    };
};
export declare const getContentContainerStyle: (maxHeaderHeight: number) => ViewStyle;
export default styles;
